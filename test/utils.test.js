
const { getDefaultMessage } = require('../src/utils');

test1('should return defaultMessage', () => {
  //expect(paramParser('baseHref=/my-repo-name/ sidebarOrganization=byTags')).toEqual({baseHref: '/my-repo-name/', sidebarOrganization: 'byTags'});
});


test2('should return an error if event not supported', () => {
 // expect(paramParser('baseHref=/my-repo-name/ sidebarOrganization=byTags')).toEqual({baseHref: '/my-repo-name/', sidebarOrganization: 'byTags'});
});